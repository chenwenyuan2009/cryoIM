module common_ort
    implicit none
    integer totalpars,mode
    parameter(totalpars=2)
    character(len=64):: coeff(totalpars),par(50)
    character(len=64):: stckfile,temp,stckfile1
    character(len=128):: ortcentfile


end module
